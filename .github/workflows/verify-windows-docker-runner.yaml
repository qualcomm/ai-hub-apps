##
## Manual smoke-test workflow to verify the self-hosted Windows Docker runner.
##
## Trigger from the Actions tab (or `gh workflow run`) against any branch.
##

name: Verify Windows Docker Runner

on:
  workflow_dispatch:

jobs:
  verify-runner:
    name: "Verify Windows Docker Runner"
    runs-on: [self-hosted, Windows, X64, windows-docker]

    steps:
    - name: Placeholder
      shell: pwsh
      run: Write-Host "Runner picked up the job."
